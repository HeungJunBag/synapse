# Auth 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 이메일+비밀번호 로그인, Middleware 기반 세션 보호, Next.js App Router route group 분리를 구현한다.

**Architecture:** `@supabase/ssr`이 쿠키 기반 세션을 처리한다. `middleware.ts`가 모든 요청에서 세션을 갱신하고 미인증 사용자를 `/login`으로 리다이렉트한다. 공개 라우트(`(auth)`)와 보호 라우트(`(app)`)를 route group으로 분리한다.

**Tech Stack:** Next.js 16 (App Router), @supabase/ssr, Supabase Auth (local), TypeScript

---

## 파일 구조

```
supabase/
  config.toml                               # Modify: 이메일 확인 비활성화

frontend/src/
  middleware.ts                             # Create: 세션 갱신 + 리다이렉트
  lib/supabase/
    client.ts                               # 기존 유지
    server.ts                               # Create: 서버 클라이언트
  app/
    page.tsx                                # Delete: (app)/page.tsx로 대체
    (auth)/
      login/
        page.tsx                            # Create: 로그인/회원가입 UI
        actions.ts                          # Create: signIn, signUp, signOut
    (app)/
      layout.tsx                            # Create: 인증 필요 레이아웃
      page.tsx                              # Create: 메인 페이지 (보호됨)
```

---

## Task 1: Supabase 이메일 확인 비활성화

**Files:**
- Modify: `supabase/config.toml`

- [ ] **Step 1: config.toml에 이메일 확인 비활성화 설정 추가**

`supabase/config.toml`의 `[auth.email]` 섹션을 찾아 `enable_confirmations = false`를 추가한다.

현재 `[auth.email]` 섹션:
```toml
[auth.email]
enable_signup = true
double_confirm_changes = true
```

수정 후:
```toml
[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = false
```

- [ ] **Step 2: Supabase 재시작으로 설정 반영**

```bash
# synapse/ 루트에서 실행
supabase stop && supabase start
```

Expected: `Started supabase local development setup.` 출력 확인

- [ ] **Step 3: 커밋**

```bash
git add supabase/config.toml
git commit -m "chore: disable email confirmations for local dev"
```

---

## Task 2: @supabase/ssr 설치 + 서버 클라이언트

**Files:**
- Modify: `frontend/package.json` (npm install로 자동)
- Create: `frontend/src/lib/supabase/server.ts`

- [ ] **Step 1: @supabase/ssr 설치**

```bash
cd frontend
npm install @supabase/ssr
```

Expected: `added N packages` 출력

- [ ] **Step 2: server.ts 작성**

`frontend/src/lib/supabase/server.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // Server Component에서 호출된 경우 무시.
            // middleware가 세션을 갱신하므로 안전.
          }
        },
      },
    }
  )
}
```

- [ ] **Step 3: TypeScript 컴파일 확인**

```bash
cd frontend
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 4: 커밋**

```bash
git add frontend/src/lib/supabase/server.ts frontend/package.json frontend/package-lock.json
git commit -m "feat: add supabase ssr server client"
```

---

## Task 3: middleware.ts — 세션 갱신 + 리다이렉트

**Files:**
- Create: `frontend/src/middleware.ts`

- [ ] **Step 1: middleware.ts 작성**

`frontend/src/middleware.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          )
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  // 세션 갱신 (IMPORTANT: getUser()를 반드시 호출해야 토큰이 갱신됨)
  const {
    data: { user },
  } = await supabase.auth.getUser()

  const { pathname } = request.nextUrl

  // 미인증 + 보호 경로 → /login 리다이렉트
  if (!user && pathname !== '/login') {
    const url = request.nextUrl.clone()
    url.pathname = '/login'
    return NextResponse.redirect(url)
  }

  // 인증됨 + /login 접근 → / 리다이렉트
  if (user && pathname === '/login') {
    const url = request.nextUrl.clone()
    url.pathname = '/'
    return NextResponse.redirect(url)
  }

  return supabaseResponse
}

export const config = {
  matcher: [
    // 정적 파일, 이미지, favicon 제외한 모든 경로
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```

- [ ] **Step 2: TypeScript 컴파일 확인**

```bash
cd frontend
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 3: 커밋**

```bash
git add frontend/src/middleware.ts
git commit -m "feat: add auth middleware for session refresh and route protection"
```

---

## Task 4: Server Actions — signIn, signUp, signOut

**Files:**
- Create: `frontend/src/app/(auth)/login/actions.ts`

- [ ] **Step 1: actions.ts 작성**

`frontend/src/app/(auth)/login/actions.ts`:

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export async function signIn(
  formData: FormData
): Promise<{ error: string } | never> {
  const supabase = await createClient()

  const { error } = await supabase.auth.signInWithPassword({
    email: formData.get('email') as string,
    password: formData.get('password') as string,
  })

  if (error) {
    return { error: error.message }
  }

  revalidatePath('/', 'layout')
  redirect('/')
}

export async function signUp(
  formData: FormData
): Promise<{ error: string } | never> {
  const supabase = await createClient()

  const { error } = await supabase.auth.signUp({
    email: formData.get('email') as string,
    password: formData.get('password') as string,
  })

  if (error) {
    return { error: error.message }
  }

  revalidatePath('/', 'layout')
  redirect('/')
}

export async function signOut(): Promise<never> {
  const supabase = await createClient()
  await supabase.auth.signOut()
  revalidatePath('/', 'layout')
  redirect('/login')
}
```

- [ ] **Step 2: TypeScript 컴파일 확인**

```bash
cd frontend
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 3: 커밋**

```bash
git add frontend/src/app/
git commit -m "feat: add auth server actions (signIn, signUp, signOut)"
```

---

## Task 5: 로그인 UI

**Files:**
- Create: `frontend/src/app/(auth)/login/page.tsx`

- [ ] **Step 1: login/page.tsx 작성**

`frontend/src/app/(auth)/login/page.tsx`:

```tsx
'use client'

import { useState } from 'react'
import { signIn, signUp } from './actions'

export default function LoginPage() {
  const [error, setError] = useState<string | null>(null)
  const [mode, setMode] = useState<'signin' | 'signup'>('signin')
  const [pending, setPending] = useState(false)

  async function handleSubmit(formData: FormData) {
    setPending(true)
    setError(null)
    const action = mode === 'signin' ? signIn : signUp
    const result = await action(formData)
    if (result?.error) {
      setError(result.error)
      setPending(false)
    }
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="w-full max-w-sm space-y-6 p-8 bg-white rounded-lg shadow-sm">
        <h1 className="text-2xl font-bold text-center">시냅스</h1>

        <form action={handleSubmit} className="space-y-3">
          <input
            name="email"
            type="email"
            placeholder="이메일"
            required
            className="w-full border rounded-md px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-black"
          />
          <input
            name="password"
            type="password"
            placeholder="비밀번호 (6자 이상)"
            required
            minLength={6}
            className="w-full border rounded-md px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-black"
          />
          {error && (
            <p className="text-red-500 text-sm">{error}</p>
          )}
          <button
            type="submit"
            disabled={pending}
            className="w-full bg-black text-white rounded-md py-2 text-sm font-medium disabled:opacity-50"
          >
            {pending ? '처리 중...' : mode === 'signin' ? '로그인' : '회원가입'}
          </button>
        </form>

        <button
          onClick={() => {
            setMode(mode === 'signin' ? 'signup' : 'signin')
            setError(null)
          }}
          className="w-full text-sm text-gray-500 underline"
        >
          {mode === 'signin'
            ? '계정이 없으신가요? 회원가입'
            : '이미 계정이 있으신가요? 로그인'}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: TypeScript 컴파일 확인**

```bash
cd frontend
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 3: 커밋**

```bash
git add frontend/src/app/\(auth\)/
git commit -m "feat: add login/signup page UI"
```

---

## Task 6: 인증 보호 레이아웃 + 메인 페이지

**Files:**
- Delete: `frontend/src/app/page.tsx` (route 충돌 방지)
- Create: `frontend/src/app/(app)/layout.tsx`
- Create: `frontend/src/app/(app)/page.tsx`

- [ ] **Step 1: 기존 app/page.tsx 삭제**

```bash
rm frontend/src/app/page.tsx
```

`(app)/page.tsx`가 `/` 경로를 대체하므로 기존 파일과 충돌을 막기 위해 삭제한다.

- [ ] **Step 2: (app)/layout.tsx 작성**

`frontend/src/app/(app)/layout.tsx`:

```tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export default async function AppLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  if (!user) {
    redirect('/login')
  }

  return <>{children}</>
}
```

- [ ] **Step 3: (app)/page.tsx 작성**

`frontend/src/app/(app)/page.tsx`:

```tsx
import { createClient } from '@/lib/supabase/server'
import { signOut } from '@/app/(auth)/login/actions'

export default async function HomePage() {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  return (
    <div className="min-h-screen p-8">
      <div className="max-w-4xl mx-auto">
        <div className="flex justify-between items-center mb-8">
          <h1 className="text-2xl font-bold">시냅스</h1>
          <div className="flex items-center gap-4">
            <span className="text-sm text-gray-500">{user?.email}</span>
            <form action={signOut}>
              <button
                type="submit"
                className="text-sm text-gray-500 underline hover:text-black"
              >
                로그아웃
              </button>
            </form>
          </div>
        </div>
        <p className="text-gray-400 text-center mt-20">
          노트 기능 구현 예정
        </p>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: TypeScript 컴파일 확인**

```bash
cd frontend
npx tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/app/
git commit -m "feat: add protected app layout and main page placeholder"
```

---

## Task 7: E2E 수동 검증

Auth 코드는 브라우저 쿠키·세션을 포함하므로 단위 테스트 대신 실행 중인 dev server로 직접 검증한다.

- [ ] **Step 1: dev server 시작**

```bash
cd frontend
npm run dev
```

Expected: `http://localhost:3000` 에서 서버 실행

- [ ] **Step 2: 미인증 접속 → /login 리다이렉트 확인**

브라우저에서 `http://localhost:3000` 접속.  
Expected: `http://localhost:3000/login` 으로 자동 리다이렉트

- [ ] **Step 3: 회원가입 확인**

`/login` 페이지에서 "회원가입" 모드로 전환 후 이메일+비밀번호 입력 후 제출.  
Expected: `http://localhost:3000/` 으로 리다이렉트, 상단에 이메일 주소 표시

- [ ] **Step 4: 로그아웃 확인**

메인 페이지에서 "로그아웃" 클릭.  
Expected: `http://localhost:3000/login` 으로 리다이렉트

- [ ] **Step 5: 로그인 확인**

`/login` 에서 동일 이메일+비밀번호로 로그인.  
Expected: `http://localhost:3000/` 으로 리다이렉트

- [ ] **Step 6: 인증 상태에서 /login 접근 차단 확인**

로그인 상태에서 `http://localhost:3000/login` 직접 접속.  
Expected: `http://localhost:3000/` 으로 자동 리다이렉트

- [ ] **Step 7: 최종 커밋**

```bash
git add -A
git commit -m "feat: complete auth setup — email/password login with middleware protection"
```

---

## 완료 기준

- [ ] `supabase/config.toml` — 이메일 확인 비활성화
- [ ] `frontend/src/lib/supabase/server.ts` — 서버 클라이언트
- [ ] `frontend/src/middleware.ts` — 세션 갱신 + 리다이렉트
- [ ] `frontend/src/app/(auth)/login/actions.ts` — signIn, signUp, signOut
- [ ] `frontend/src/app/(auth)/login/page.tsx` — 로그인 UI
- [ ] `frontend/src/app/(app)/layout.tsx` — 인증 보호 레이아웃
- [ ] `frontend/src/app/(app)/page.tsx` — 메인 페이지
- [ ] E2E 7개 시나리오 모두 통과
